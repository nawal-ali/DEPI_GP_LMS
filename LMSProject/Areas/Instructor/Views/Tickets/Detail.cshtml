@using LMSProject.ViewModels.Tickets
@model TicketDetailVM
@{
    ViewData["Title"] = "Ticket #" + Model.Ticket.Id;
    ViewData["BackUrl"] = Url.Action("Index", "Tickets", new { area = "Instructor" });
    ViewData["StatusUrl"] = Url.Action("ChangeStatus", "Tickets", new { area = "Instructor" });
    ViewData["ForwardUrl"] = "";
}

@await Html.PartialAsync("_TicketThread", Model)
