@using LMSProject.Areas.Admin.ViewModel
@model List<SubSubjectIndexVM>
@{
    ViewData["Title"] = "Sub-Subjects";
}

<div class="d-flex justify-content-between align-items-center mb-4">
    <h5 style="font-weight:700;color:var(--primary);margin:0;">
        <i class="fas fa-bookmark me-2" style="color:var(--secondary);"></i>Sub-Subjects
        <span class="sa-badge sa-badge-info ms-2">@Model.Count</span>
    </h5>
    <a asp-area="Admin" asp-controller="SubSubject" asp-action="Create"
       class="btn btn-primary btn-sm rounded-3 px-3">
        <i class="fas fa-plus me-1"></i> Add Sub-Subject
    </a>
</div>

<div class="sa-card">
    <div class="table-responsive">
        <table class="sa-table">
            <thead>
                <tr>
                    <th>Image</th>
                    <th>Sub-Subject</th>
                    <th>Parent Subject</th>
                    <th>Status</th>
                    <th>Actions</th>
                </tr>
            </thead>
            <tbody>
                @foreach (var sub in Model)
                {
                    <tr>
                        <td>
                            @if (!string.IsNullOrEmpty(sub.ImageName))
                            {
                                <img src="~/@sub.ImageName"
                                     style="width:48px;height:48px;border-radius:10px;object-fit:cover;" />
                            }
                            else
                            {
                                <div style="width:48px;height:48px;border-radius:10px;background:linear-gradient(135deg,var(--accent-purple),var(--accent-pink));display:flex;align-items:center;justify-content:center;">
                                    <i class="fas fa-bookmark" style="color:rgba(255,255,255,0.7);"></i>
                                </div>
                            }
                        </td>
                        <td style="font-weight:600;">@sub.Name</td>
                        <td>
                            <span class="sa-badge sa-badge-purple">@sub.Subject</span>
                        </td>
                        <td>
                            @if (sub.CurrentState == 1)
                            {
                                <span class="sa-badge sa-badge-active">Active</span>
                            }
                            else
                            {

                                <span class="sa-badge sa-badge-inactive">Inactive</span>
                            }
                        </td>
                        <td>
                            <div class="d-flex gap-2">
                                <a asp-area="Admin" asp-controller="SubSubject" asp-action="Edit"
                                   asp-route-Id="@sub.Id"
                                   class="sa-btn-icon sa-btn-edit" title="Edit">
                                    <i class="fas fa-edit"></i>
                                </a>
                                <button type="button"
                                        class="sa-btn-icon sa-btn-delete" title="Delete"
                                        onclick="confirmDelete('/Admin/SubSubject/Delete?Id=@sub.Id', '@sub.Name')">
                                    <i class="fas fa-trash"></i>
                                </button>
                            </div>
                        </td>
                    </tr>
                }
                @if (!Model.Any())
                {
                    <tr>
                        <td colspan="5" class="text-center py-5" style="color:#aaa;">
                            <i class="fas fa-bookmark fa-2x d-block mb-2 opacity-25"></i>No sub-subjects yet.
                        </td>
                    </tr>
                }
            </tbody>
        </table>
    </div>
</div>

@section Scripts {
    <script>
        function confirmDelete(url, name) {
            if (confirm('Are you sure you want to delete "' + name + '"?\nThis action cannot be undone.')) {
                window.location.href = url;
            }
        }
    </script>
}
